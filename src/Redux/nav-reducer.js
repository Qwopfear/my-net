
let initialState = {
    BestFriends: [
        { id: 1, name: 'Shakal' },
        { id: 2, name: 'Pes' },
        { id: 3, name: 'Wolk' },
        { id: 4, name: 'Lisa' }
    ]
}

const navReducer = (state = initialState, action) => {
    switch (action.type) {

        default: return state
    }

}


export default navReducer;